

when compiling the program you need to end the gcc with -lpcap

when compiling the program also use -lsystemd


adjust message buffer to reallocate memory size to the length of MESSAGE field , in logs.c

create a file which is written to when the server runs, appending the date and time of a server log
(completed above) 

create a file which will detect brute force attempts

create a function  which checks for  errors which runs in the follow journal logs function , which then calls the write file function and get time function to put a time descriptive log in a text file . 
(completed above)


RUN TWO SIMULATENOUS THREADS ONE FOR THE PCAP LOOP AND ONE FOR THE SYSTEMD LOG LOOP 
(completed above)  


complete functionality to check for arp poisoning, and then ban that mac address from connecting to our ting





-------------------------------------------------------------------------------------------
how to run ettercap
sudo ettercap -T -S -i ETH -M arp:remote /*.*.*.*// /*.*.*.*// (last 4 octets destination)  
